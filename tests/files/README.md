# Test fixture provenance

`mnd_export.xls` — synthetic MyNetDiary export.
All values are fabricated; no real personal data.
Tests the `.xls` read path, since `openpyxl` can only be used to build `.xlsx` files in `tests/mnd_fixtures.py`.
